{
  "$schema": "http://json-schema.org/draft-07/schema#",
  "title": "PETC Photo Presign Response",
  "description": "Response from POST /api/photos/presign. The desktop uses uploadUrl to PUT photo bytes directly to S3.",
  "type": "object",
  "required": ["s3Key", "uploadUrl"],
  "additionalProperties": false,
  "properties": {
    "s3Key":     { "type": "string", "description": "Canonical S3 object key: tenants/{tenantId}/tests/{testId}/{photoId}.jpg" },
    "uploadUrl": { "type": "string", "format": "uri", "description": "Presigned S3 PUT URL valid for the configured TTL (default 300s)" }
  }
}
